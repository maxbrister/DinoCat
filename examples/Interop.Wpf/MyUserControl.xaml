<UserControl x:Class="Interop.Wpf.MyUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Interop.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" x:Name="root">
    <Grid>
        <TextBlock>
            <TextBlock.Inlines>
                <Run>Hello from Wpf. MyUserControl is embeded into DinoCat Count:</Run>
                <Run Text="{Binding ElementName=root, Path=MyValue}"/>
                <Run>Id:</Run>
                <Run Text="{Binding ElementName=root, Path=Id}"/>
            </TextBlock.Inlines>
        </TextBlock>
    </Grid>
</UserControl>
